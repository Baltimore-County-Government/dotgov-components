import React from "react";
import Button from "./Button";
const Modal = props => {
  return (
    <div class="container">
      <Button text="NO" />
      <h2>Modal Example</h2>
    </div>
  );
};

export default Modal;
